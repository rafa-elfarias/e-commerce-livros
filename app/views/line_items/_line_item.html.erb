<% if line_item == @current_item %>
<tr id="current_item" >
<% else %>
<tr>
<td><%= line_item.quantity %>&times;</td>
<td><%= line_item.product.title %></td><br />
<td class="item_price" ><%= number_to_currency(line_item.total_price) %></td>
</tr>
<% end %>
<!--<%= sprintf("%2d x %s",
            line_item.quantity,
            truncate(line_item.product.title, :length => 50)) %>--!>
            
